% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_plot.R
\name{event_plot}
\alias{event_plot}
\title{Event-study plot for a did_impute result}
\usage{
event_plot(
  results_obj = NULL,
  pretrends = NULL,
  pretrends_std = NULL,
  effects = NULL,
  effects_std = NULL,
  plot_type = c("rcap", "rarea"),
  significance_level = 0.05,
  together = FALSE,
  xlab = "Time Relative to Treatment",
  ylab = "Coefficient",
  title = NULL,
  pretrends_color = "blue",
  effects_color = "red",
  ...
)
}
\arguments{
\item{results_obj}{A \code{did_impute} result object, or \code{NULL}.}

\item{pretrends}{Named list of pre-trend estimates (keys \code{pre<k>}).
Used only when \code{results_obj} is \code{NULL}.}

\item{pretrends_std}{Named list of pre-trend standard errors.}

\item{effects}{Named list of effect estimates (keys \code{tau<k>}).}

\item{effects_std}{Named list of effect standard errors.}

\item{plot_type}{\code{"rcap"} (error bars, default) or \code{"rarea"}
(shaded ribbon).}

\item{significance_level}{Significance level for CIs (default 0.05, giving
95 percent CIs).}

\item{together}{Logical; if \code{TRUE} pre-trends and effects are combined
into a single series (default \code{FALSE}).}

\item{xlab}{X-axis label.}

\item{ylab}{Y-axis label.}

\item{title}{Optional plot title.}

\item{pretrends_color}{Colour for pre-trend series (default \code{"blue"}).}

\item{effects_color}{Colour for effects series (default \code{"red"}).}

\item{...}{Currently unused.}
}
\value{
A \code{ggplot} object.  The underlying data frame (accessible as
\code{p$data}) contains columns \code{x}, \code{coef}, \code{err}, and
\code{group}.
}
\description{
Produces a ggplot2 event-study figure from a \code{did_impute} result object
or from explicitly supplied coefficient and standard-error lists.
Pre-trend estimates keyed \code{pre<k>} are placed at \eqn{x = -k}; horizon
estimates keyed \code{tau<k>} at \eqn{x = +k}.  Confidence intervals use the
critical value \eqn{z_{1-\alpha/2}}.
}
\examples{
\dontrun{
res <- did_impute(df, y = "y", i = "i", t = "t", Ei = "Ei",
                  horizons = 0:2, pretrends = 2)
event_plot(res)
event_plot(res, plot_type = "rarea", together = TRUE)
}
}
