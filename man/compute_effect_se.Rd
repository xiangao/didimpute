% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standard_errors.R
\name{compute_effect_se}
\alias{compute_effect_se}
\title{Compute cluster-robust standard errors via BJS smartweights}
\usage{
compute_effect_se(
  dt,
  wtr,
  y,
  cluster,
  avgeffectsby,
  gr_var,
  fe,
  controls = character()
)
}
\arguments{
\item{dt}{data.table (will be modified; pass a copy)}

\item{wtr}{character vector of original treatment-weight column names}

\item{y}{name of outcome column}

\item{cluster}{name of cluster column}

\item{avgeffectsby}{character vector of columns defining the estimand group}

\item{gr_var}{character vector = union of cluster + avgeffectsby}

\item{fe}{character vector of fixed-effect columns (passed to imputation_weights)}

\item{controls}{character vector of control variable names (default empty)}
}
\value{
list with \code{se} (named numeric), \code{group_sums} matrix, \code{V} matrix
}
\description{
Faithful port of Python \code{compute_effect_se} (did_imputation.py lines 615-711,
no-controls path). Operates on a COPY of dt (caller must pass one).
}
\details{
For each weight column in \code{wtr}:
\itemize{
\item Fill NaN copy\if{html}{\out{<w>}} with original w (non-iterated weights)
\item Compute smartweights on treated rows
\item Residual: y - y_hat (all rows), overridden to tau - avg_taus (treated rows)
\item product = resid * copy\if{html}{\out{<w>}} * wei
\item group_sums = sum(product) per cluster
Then V = G'G where G = cbind(group_sums); ses = sqrt(diag(V)).
}
}
\keyword{internal}
